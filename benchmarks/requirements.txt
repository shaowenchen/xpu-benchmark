# XPU Benchmark Requirements
# Core dependencies
numpy>=1.21.0
psutil>=5.8.0
pyyaml>=6.0
matplotlib>=3.5.0
pandas>=1.3.0
seaborn>=0.11.0

# Jupyter and development
jupyter>=1.0.0
notebook>=6.4.0

# Testing and code quality
pytest>=6.2.0
pytest-cov>=2.12.0
pytest-mock>=3.6.0
black>=21.0.0
flake8>=3.9.0
mypy>=0.910

# GPU dependencies (NVIDIA)
# PyTorch - install with CUDA support if available
torch>=1.12.0
torchvision>=0.13.0
torchaudio>=0.12.0

# TensorFlow - install with GPU support if available
tensorflow>=2.8.0

# AI/ML frameworks
transformers>=4.20.0
datasets>=2.0.0
accelerate>=0.12.0
deepspeed>=0.6.0

# GPU monitoring
nvidia-ml-py3>=7.352.0

# NPU dependencies (Huawei Ascend)
# MindSpore - install according to Huawei's official guide
# mindspore>=1.8.0
# mindinsight>=1.8.0
# mindarmour>=1.8.0
# mindspore-hub>=1.8.0

# Development tools
pre-commit>=2.17.0
coverage>=6.0.0
sphinx>=4.0.0
sphinx-rtd-theme>=1.0.0
twine>=3.7.0

# Optional: Additional GPU optimization libraries
# apex  # Install manually if needed
# nvidia-dali  # Install manually if needed 