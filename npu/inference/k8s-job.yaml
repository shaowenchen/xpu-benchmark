apiVersion: batch/v1
kind: Job
metadata:
  name: npu-inference-test
  namespace: default
  labels:
    app: npu-benchmark
    type: inference
spec:
  template:
    metadata:
      labels:
        app: npu-benchmark
        type: inference
    spec:
      restartPolicy: Never
      containers:
      - name: npu-inference
        image: shaowenchen/xpu-benchmark:npu-inference-latest
        imagePullPolicy: Always
        env:
        - name: NVIDIA_VISIBLE_DEVICES
          value: "all"
        - name: NVIDIA_DRIVER_CAPABILITIES
          value: "compute,utility"
        resources:
          limits:
            nvidia.com/gpu: 1
          requests:
            nvidia.com/gpu: 1
        volumeMounts:
        - name: logs-volume
          mountPath: /app/logs
        - name: results-volume
          mountPath: /app/results
        - name: config-volume
          mountPath: /app/config.yaml
          subPath: config.yaml
      volumes:
      - name: logs-volume
        persistentVolumeClaim:
          claimName: npu-logs-pvc
      - name: results-volume
        persistentVolumeClaim:
          claimName: npu-results-pvc
      - name: config-volume
        configMap:
          name: npu-inference-config
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: npu-inference-config
  namespace: default
data:
  config.yaml: |
    # NPU Inference Configuration
    model: bert
    framework: mindspore
    batch_size: 16
    iterations: 100
    device: npu
    log_level: INFO 